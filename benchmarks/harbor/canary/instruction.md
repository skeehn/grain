Use shell output redirection (`>`) to create `/app/grain-canary.txt` containing
exactly this single line:

```text
grain-harbor-ok
```

Do not create or modify any other file. Verify the file contents before you finish.
